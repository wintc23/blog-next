node_modules
.next
out
.env*.local
.env.playwright
next-env.d.ts
.DS_Store
*.tsbuildinfo
# `scripts/` holds developer-only tooling (Playwright probes, admin
# CLI, throwaway env files) — never commit anything from here.
scripts/
